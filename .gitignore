# Licence-restricted financial series — regenerate with scripts/fetch_financial_series.py
data/raw/financial/
data/clean/uk_stock_returns_gbp.csv

# The built calculator IS committed, so that the tool can simply be downloaded
# and opened. It embeds derived monthly returns, which output/tables already
# publishes; MSCI's raw index levels stay out of the repository.

# Python
__pycache__/
*.pyc
.venv/

# OS
.DS_Store
Thumbs.db
